# Installer dependenceies

You need install Clang toolchains to compile the Carla & Morgana compilers in Windows.

* Download by winget
```sh-session
$ winget install -e --id MartinStorsjo.LLVM-MinGW.UCRT
```
